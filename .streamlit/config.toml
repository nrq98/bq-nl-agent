[server]
address = "localhost"   # solo acepta conexiones locales; nunca exponer en 0.0.0.0
port = 8501
headless = true         # no abre el navegador automáticamente en entornos sin GUI

[browser]
gatherUsageStats = false  # no envía telemetría a Streamlit
